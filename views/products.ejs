<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8" />
  <title>Produkty</title>
  <style>
    body { font-family: sans-serif; padding: 2rem; }
    .product { border-bottom: 1px solid #ccc; padding: 1rem 0; }
    .product h2 { margin: 0 0 0.5rem; }
  </style>
</head>
<body>
  <h1>Naše produkty</h1>
  <% products.forEach(p => { %>
    <div class="product">
      <h2><%= p.name %></h2>
      <p><%= p.description || '' %></p>
      <strong><%= p.price %> Kč</strong>
    </div>
  <% }); %>
  <p><a href="/">Zpět na úvod</a></p>
</body>
</html>
