<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8" />
  <title>Produkty</title>
  <link rel="stylesheet" href="/styles.css" />
  <a href="/cart">Zobrazit košík</a>

</head>
<body>
  <div class="container">
    <header>
      <h1>Naše produkty</h1>
    </header>
    <% products.forEach(p => { %>
      <div class="product">
        <h2><%= p.name %></h2>
        <p><%= p.description || '' %></p>
        <strong><%= p.price %> Kč</strong>
        <form action="/cart" method="POST" style="margin-top: 1rem;">
          <input type="hidden" name="productId" value="<%= p.id %>" />
          <button type="submit">Přidat do košíku</button>
        </form>
      </div>
    <% }); %>
    
    <a href="/">Zpět na úvod</a>
  </div>
</body>
</html>
